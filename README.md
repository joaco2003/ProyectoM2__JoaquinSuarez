# MiniBlog API - DevSpark Backend

API REST construida con Node.js, Express y PostgreSQL para la gestión de usuarios (authors), publicaciones (posts) y comentarios (comments).

## 🛠️ Ejecución Local

1. Instala las dependencias:
   ```bash
   npm install